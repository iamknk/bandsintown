import React from 'react';
import { shallow } from 'enzyme';
import { Spinner } from '../../index';


let wrapped = shallow(<Spinner />);
describe('Spinner', () => {
 
  it('renders the spinner', () => { 
  });
});